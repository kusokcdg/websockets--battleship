Websocket battleship developed with typescript and implemented part requirements. websockets-server implemented to part start game

1. clone repo
2. "npm install" from './websockets-ui'
3. "npm install" from './websockets-server'
4. "npm run start" from './websockets-ui'
5. "npm run start" from './websockets-server'